# Opening a Connection always creates a new Session

Two windows on the same Connection are two Sessions with independent Staged Changes. Focusing an existing Session instead would block side-by-side views of one Database. Sharing one bag across windows would require sync we will not build.
