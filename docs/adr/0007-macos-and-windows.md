# v1 ships on macOS and Windows, not Linux

The known handful of people who will use the Client are on macOS and Windows. GPUI also targets Linux, but a Linux build is extra packaging and font/window work with nobody in that group on Linux. Linux waits.
