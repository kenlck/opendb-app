# Create table uses the Namespace you are browsing

A create-table Schema Change lands in the Namespace whose catalog you are looking at. There is no extra Namespace field and no silent fall-through to `public`. Putting a Table in another Namespace is SQL.
