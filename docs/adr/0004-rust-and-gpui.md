# Native desktop Client on Rust and GPUI

We are building a native desktop Client in Rust on GPUI, not Electron, a web app, or another Rust UI kit. GPUI matches the TablePlus-native feel we want and is a given for this repo. It is pre-1.0 with breaking changes; that lock-in is accepted.
