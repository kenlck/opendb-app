# Unlimited Tabs, one Staged Change bag per Session

A Session can have unlimited Tabs (Table grids, Queries, structure). Apply sends every Staged Change on that Session, from any Tab, in one transaction. Closing a Tab does not discard Staged Changes. Per-tab Apply is rejected.
