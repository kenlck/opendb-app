# One Session per OS window, unlimited windows

Each Session is a native window. Two Databases on screen means two windows. Unlimited. Split panes are not v1. Opening a Connection creates a Session in a new window.
