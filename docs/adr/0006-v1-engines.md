# v1 Engines are Postgres, SQLite, and MySQL

The Client must speak more than Postgres because the handful of people who will use it are not on one Engine. v1 Engines are Postgres, SQLite files, and MySQL/MariaDB. Turso / remote libSQL is out of v1; SQLite means a local file.
