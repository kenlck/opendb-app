# The Client has no read-only Connections

Write permission is the Database user’s problem. The Client will not flag a Connection as read-only. Sharing a Connection List with a write-capable Connection String means the recipient can Apply if the Database allows it.
