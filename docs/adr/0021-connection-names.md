# Connections have a Name; the string stays hidden in the list

The Connection List shows a Name (default: Database name or file stem), not the Connection String. Secrets appear only when editing or copying. Identity for merge and uniqueness is still the Connection String. Names may collide.
