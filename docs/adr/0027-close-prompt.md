# Closing a Session with Staged Changes prompts; they are not saved

Close asks Apply, discard, or cancel. Staged Changes are never written next to the Connection List. A restored-on-Monday mutation journal is rejected.
