# v1 opens only local and directly reachable Databases

v1 connects to Databases on the machine or on the network without a tunnel, bastion, or cloud IAM flow. SSH and jump hosts are TablePlus-shaped work and a safety surface we have not designed; anyone who needs them keeps using an existing forward.
