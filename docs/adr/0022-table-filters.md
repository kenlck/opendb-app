# Table grids have simple unlimited Filters

Seeking in a Table is column Filters (equals, contains, null), with no cap. They are not a query builder and not a Query. A Query is still how you join, aggregate, or write arbitrary SQL. Filters plus paging are how you look at a large Table without `SELECT *`.
