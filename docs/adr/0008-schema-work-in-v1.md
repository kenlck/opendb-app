# v1 includes schema work

Replacing TablePlus for this group means the Client can change tables, columns, and indexes, not only rows. Schema work is in v1. How it is edited (visual designer vs SQL) and whether it goes through Staged Changes is still open.
