# Cargo's built-in git client ignores insteadOf rewrites. Use the git CLI so
# CI (and local setups) can map ssh://git@github.com/ → https:// without a
# deploy key while keeping the Zed gpui [patch] pin.
[net]
git-fetch-with-cli = true
